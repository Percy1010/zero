---
title: 令牌(Token)
description: 解释 Token 是什么、它为什么同时影响成本和上下文窗口、复杂 Agent 为什么特别烧 Token，以及普通用户现在就能采取哪些降本方法。
---

# 令牌(Token)

## 一句话定义

`Token` 是模型处理文本时使用的基本单位，也是很多 AI 服务计费和计算上下文长度的基础单位。

## 通俗理解

所以 Token 既像「阅读成本」，也像「生成成本」。

## 为什么 Token 很重要

它同时决定两件很现实的事:

1. 成本
2. 上下文窗口

## 你现在只需要记住什么

- Token 是模型处理文本和计费的基本单位
- AI 贵不贵、能看多长内容，都和 Token 直接相关
- 很多工程优化，本质上都是在「让有限的 Token 更值钱」
