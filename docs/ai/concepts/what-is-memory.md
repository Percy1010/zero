---
title: 记忆(Memory)
description: 解释 AI 里的 Memory 是什么、它和 Context 的区别、短期记忆与长期记忆怎么理解，以及普通用户现在就能采用哪些记忆策略。
---

# 记忆(Memory)

## 一句话定义

`Memory` 是让原本每次都像「第一次见你」的模型，能够把重要信息保存下来，并在后续继续使用的一套机制。

## 通俗理解

模型天生更像「金鱼记忆」: 你不把旧信息重新给它，它就不会自动记住。  
Memory 机制就像给这条金鱼配了一本日记本。

## 它的工作原理

1. 把对话历史或关键信息存起来
2. 在下一次调用前，取出相关内容
3. 再把它们放进新的 Context

所以 Memory 的本质不是「模型脑子里记住了」，而是「系统在帮它做外部记忆管理」。

## 你现在只需要记住什么

- Memory 不是普通聊天记录的同义词
- 它的本质是「把以后还要用的信息保存下来」
- 不是记得越多越好，而是记得越准越好
